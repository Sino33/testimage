{% extends 'base.html.twig' %}

{% block title %}Votre panier{% endblock %}

{% block body %}






    <div class="alert alert-warning">
        <h2>{{user|capitalize}}, Voici votre historique des commandes</h2>
    </div>
<div class="container">
<table class="table table-striped table-dark">
  <thead>
    <tr>
      <th scope="col">Commande</th>
      <th scope="col">Prix</th>
      <th scope="col">Taille</th>
    </tr>
  </thead>
  <tbody>

{% for article in listPanier %}
    <tr>
      <td>{{article.commande}}</td>
      <td>{{article.commande.prix}}</td>
      <td>{{article.commande.taille}}</td>
{% endfor %}
    </tr>
  </tbody>
</table>
</div>

{# {{ dump(user)}}
{{ dump(panier)}} #}







{% endblock %}